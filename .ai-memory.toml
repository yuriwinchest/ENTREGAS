workspace = "clientes"
project_strategy = "repo-root"

[briefing]
inject_on_session_start = "true"
max_chars = 4000

[capture]
ignore_paths = [
  ".env",
  ".env.*",
  "**/*.pem",
  "**/*.key",
  "secrets/**",
  "private/**",
  "backups/**",
  "logs/**"
]
